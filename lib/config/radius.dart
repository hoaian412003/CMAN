import 'package:flutter/material.dart';

class RadiusConfig {
  static const tiny = Radius.circular(4.0);
  static const small = Radius.circular(8.0);
  static const medium = Radius.circular(12.0);
}
